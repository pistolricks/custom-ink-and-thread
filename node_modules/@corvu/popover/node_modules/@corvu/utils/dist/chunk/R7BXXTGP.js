import { style_default } from './HD4B7J7A.js';
import { access } from './ZV6G25TT.js';
import { mergeProps, createEffect } from 'solid-js';

var createNoPointerEvents = (props) => {
  const defaultedProps = mergeProps(
    {
      enabled: true
    },
    props
  );
  createEffect(() => {
    const { body } = document;
    if (!access(defaultedProps.enabled)) return;
    style_default({
      key: "no-pointer-events",
      element: body,
      style: {
        pointerEvents: "none"
      }
    });
  });
};
var noPointerEvents_default = createNoPointerEvents;

export { noPointerEvents_default };
