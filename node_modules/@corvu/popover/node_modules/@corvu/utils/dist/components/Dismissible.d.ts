import { Component, JSX } from 'solid-js';
import { CreateDismissableProps } from '../create/dismissible.js';
import '../types-CfOU1RES.js';

type DismissibleProps = {
    /**
     * Whether the dismissible is enabled.
     * @defaultValue `true`
     */
    enabled: boolean;
    children: JSX.Element | ((props: {
        isLastLayer: boolean;
    }) => JSX.Element);
} & CreateDismissableProps;
/** A component that can be dismissed by pressing the escape key or clicking outside of it. Can be nested. */
declare const Dismissible: Component<DismissibleProps>;

export { Dismissible as default };
